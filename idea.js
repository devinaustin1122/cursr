// const defaults

// factory and composition function for creating particles

// bind appropriate follow/spawn function to onmousemove

Mouse({ selector: "myDiv", cursor: "cursor.svg" }).follow({ img: "img.svg" });
